language: node_js
node_js:
  - "8"
sudo: false

branches:
  only:
    - master

cache:
  directories:
    - node_modules

addons:
  chrome: stable

before_script:
  - npm install angular-cli
  - npm install

script:
  - ng build --prod
  - ng lint
  - ng test
  - ng e2e

